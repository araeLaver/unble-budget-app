# ==============================================================
# DEVELOPMENT PROFILE - POSTGRESQL (KOYEB) - unble_dev 스키마
# ==============================================================

# PostgreSQL Database Configuration (Development)
spring.datasource.url=jdbc:postgresql://ep-blue-unit-a2ev3s9x.eu-central-1.pg.koyeb.app/untab?user=untab&password=0AbVNOIsl2dn&sslmode=require&currentSchema=unble_dev
spring.datasource.username=untab
spring.datasource.password=0AbVNOIsl2dn
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Settings (개발용)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.format-sql=true
spring.jpa.properties.hibernate.default_schema=unble_dev
spring.jpa.properties.hibernate.dialect.default_schema=unble_dev
spring.jpa.defer-datasource-initialization=true

# SQL Scripts
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:data-dev.sql
spring.sql.init.continue-on-error=true

# SSL
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslmode=require

# Logging
logging.level.com.unble.budget=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE